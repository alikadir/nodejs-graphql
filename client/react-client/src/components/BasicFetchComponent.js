import React from 'react';

const click = e => {
  console.log('cliccckkk');
  // get data via Apollo client in Apollo Context
};

export default props => <button onClick={click}>Basic Fetch Data</button>;
